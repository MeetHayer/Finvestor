# Backend
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
CORS_ORIGINS=http://localhost:5173

# Data constraints
MAX_LOOKBACK_DAYS=372    # ~1y incl. weekends/holidays

# Throttle
YF_MAX_CONCURRENCY=2
YF_MIN_DELAY_MS=1200

# Optional: Supabase (if you want persistence beyond process)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_TABLE=price_daily   # created automatically if missing
